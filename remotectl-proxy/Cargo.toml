[package]
name = "remotectl-proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
remotectl-common = { path = "../remotectl-common" }
rocket = { version = "0.5.1", features = ["json"] }

[lints]
workspace = true
